import { createSlice } from '@reduxjs/toolkit';

const initialState = {
}

export const articleListSlice = createSlice({
  name: 'articleList',
  initialState,
  reducers: {

  }
})

export default articleListSlice.reducer
export const {  } = articleListSlice.actions